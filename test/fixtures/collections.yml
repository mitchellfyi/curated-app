# == Schema Information
#
# Table name: collections
#
#  id         :uuid             not null, primary key
#  domain     :string
#  keyphrases :text             default([]), is an Array
#  name       :string
#  subdomain  :string
#  created_at :datetime         not null
#  updated_at :datetime         not null
#
current_tenant:
  domain: current_tenant.domain
  subdomain: current_tenant
